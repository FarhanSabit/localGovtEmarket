<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Member</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-control, .form-select {
            border-radius: 8px;
        }
        .btn-primary {
            background-color: #007bff;
            border: none;
            border-radius: 8px;
        }
        .btn-secondary {
            border-radius: 8px;
        }
        .card-title {
            font-weight: bold;
            color: #333;
        }
        .form-label {
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title text-center mb-4">Update Member (NIN) Form</h4>
                        <form action="/editMember/<%= member ? member.id : '' %>" method="POST">
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label for="f_name" class="form-label">First Name:</label>
                                    <input type="text" id="f_name" name="f_name" class="form-control" placeholder="Enter First Name" value="<%= member ? member.f_name : '' %>" required />
                                </div>
                                <div class="col-md-6">
                                    <label for="l_name" class="form-label">Last Name:</label>
                                    <input type="text" id="l_name" name="l_name" class="form-control" placeholder="Enter Last Name" value="<%= member ? member.l_name : '' %>" />
                                </div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label for="email" class="form-label">Email:</label>
                                    <input type="email" id="email" name="email" class="form-control" placeholder="Enter Email" value="<%= member ? member.email : '' %>" />
                                </div>
                                <div class="col-md-6">
                                    <label for="phone_no" class="form-label">Phone No:</label>
                                    <input type="text" id="phone_no" name="phone_no" class="form-control" placeholder="Enter Mobile No" value="<%= member ? member.phone_no : '' %>" />
                                </div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label for="nin" class="form-label">NIN:</label>
                                    <input type="text" id="nin" name="nin" class="form-control" placeholder="Enter NIN" value="<%= member ? member.nin : '' %>" required />
                                </div>
                                <div class="col-md-6">
                                    <label for="issued_card" class="form-label">Issued Card:</label>
                                    <input type="text" id="issued_card" name="issued_card" class="form-control" placeholder="Enter Card Number" value="<%= member ? member.issued_card : '' %>" />
                                </div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label for="gender" class="form-label">Gender:</label>
                                    <select id="gender" class="form-select" name="gender">
                                        <option value="">Select Gender</option>
                                        <option value="Male" <%= member && member.gender === 'Male' ? 'selected' : '' %>>Male</option>
                                        <option value="Female" <%= member && member.gender === 'Female' ? 'selected' : '' %>>Female</option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label for="occupation" class="form-label">Occupation:</label>
                                    <select id="occupation" class="form-select" name="occupation">
                                        <option value="">Select Occupation</option>
                                        <option value="Business" <%= member && member.occupation === 'Business' ? 'selected' : '' %>>Business</option>
                                        <option value="Public Service" <%= member && member.occupation === 'Public Service' ? 'selected' : '' %>>Public Service</option>
                                        <option value="Private Service" <%= member && member.occupation === 'Private Service' ? 'selected' : '' %>>Private Service</option>
                                        <option value="Self-Employed" <%= member && member.occupation === 'Self-Employed' ? 'selected' : '' %>>Self-Employed</option>
                                        <option value="Others" <%= member && member.occupation === 'Others' ? 'selected' : '' %>>Others</option>
                                        <option value="Aspirant" <%= member && member.occupation === 'Aspirant' ? 'selected' : '' %>>Aspirant</option>
                                    </select>
                                </div>
                            </div>
                            <div class="row mb-4">
                                <div class="col-md-6">
                                    <label for="floor_type" class="form-label">Floor Type:</label>
                                    <select id="floor_type" class="form-select" name="floor_type">
                                        <option value="">Select Floor:</option>
                                        <option value="Ground Floor" <%= member && member.floor_type === 'Ground Floor' ? 'selected' : '' %>>Ground Floor</option>
                                        <option value="Level 1" <%= member && member.floor_type === 'Level 1' ? 'selected' : '' %>>Level 1</option>
                                        <option value="Level 2" <%= member && member.floor_type === 'Level 2' ? 'selected' : '' %>>Level 2</option>
                                        <option value="Level 3" <%= member && member.floor_type === 'Level 3' ? 'selected' : '' %>>Level 3</option>
                                        <option value="Level 4" <%= member && member.floor_type === 'Level 4' ? 'selected' : '' %>>Level 4</option>
                                        <option value="Food Court" <%= member && member.floor_type === 'Food Court' ? 'selected' : '' %>>Food Court</option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label for="shop_no" class="form-label">Shop No:</label>
                                    <select id="shop_no" class="form-select" name="shop_no">
                                        <option value="">Select Shop:</option>
                                        <option value="A1" <%= member && member.shop_no === 'A1' ? 'selected' : '' %>>A1</option>
                                        <option value="A2" <%= member && member.shop_no === 'A2' ? 'selected' : '' %>>A2</option>
                                        <option value="A3" <%= member && member.shop_no === 'A3' ? 'selected' : '' %>>A3</option>
                                        <option value="A4" <%= member && member.shop_no === 'A4' ? 'selected' : '' %>>A4</option>
                                        <option value="A5" <%= member && member.shop_no === 'A5' ? 'selected' : '' %>>A5</option>
                                        <option value="A6" <%= member && member.shop_no === 'A6' ? 'selected' : '' %>>A6</option>
                                    </select>
                                </div>
                            </div>
                            <div class="d-flex justify-content-between">
                                <button type="submit" class="btn btn-primary">Update</button>
                                <a href="/ninMember" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
